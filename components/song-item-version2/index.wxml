<!--components/song-item-version2/index.wxml-->
<view class="item" bindtap="handleSongItemClick">
    <view class="index">{{index}}</view>
    <view class="info">
        <view class="name">{{item.name}}</view>
        <view class="source">
            <view class="icon"></view>
            <text class="text">{{item.ar[0].name}} · {{item.alia[0]}}</text>
        </view>
    </view>
</view>